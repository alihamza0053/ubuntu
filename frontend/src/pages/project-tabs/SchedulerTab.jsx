/** Placeholder — APScheduler + cron builder UI ship in Phase 2. */
export default function SchedulerTab() {
  return (
    <div className="card text-center py-12">
      <p className="text-4xl mb-3">⏰</p>
      <h3 className="font-semibold text-lg">Scheduler coming in Phase 2</h3>
      <p className="text-sm text-slate-500 mt-2 max-w-md mx-auto">
        You'll be able to schedule any script with a visual cron builder,
        toggle schedules on/off and see last/next run times here.
      </p>
    </div>
  )
}
